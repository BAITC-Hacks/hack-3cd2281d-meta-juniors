# Career Quest · Meta Juniors

AI-навигатор развития сотрудников для кейса Halyk Bank, HackAlem AI 2026.

Профиль и история → актуальные навыки → карьерная цель → 1–3 объяснимых шага → личный план → обновление прогресса. HR получает срез дефицитов и вовлечённости. Дополнительные профили жюри загружаются через предпросмотр и подтверждение.

## Что доступно во втором этапе

- «Мой план»: добавить рекомендацию, начать, завершить; убрать ещё не начатый шаг. Повторные запросы не создают дубликаты и не начисляют навыки повторно.
- Подробности мероприятия: даты, формат, нагрузка, требования, прогноз изменения навыков и факты, объясняющие пользу для цели.
- Начатые активности из исходной истории и завершения предыдущего демо сохраняются в плане. Смена цели сохраняет план и заново проверяет пригодность ещё не начатых шагов.
- HR-фильтры по подразделению, грейду, дефициту навыка, сигналу поддержки и имени/ID/роли. Список и сводные показатели относятся к одной выборке; нажатие на дефицит открывает соответствующих сотрудников.
- Импорт: новые / обновляемые / неизменённые записи до сохранения, затем ссылки на затронутые профили. Предпросмотр действует 30 минут и доступен только создавшему его HR.

## Быстрый запуск

Требуется Docker Desktop / Docker Engine с Compose. Из корня репозитория:

```sh
docker compose up --build -d
```

Открыть http://localhost:8000. Первый запуск применяет миграции, импортирует `data/` и создаёт демо-аккаунты. Повторный запуск сохраняет данные в PostgreSQL volume.

| Роль | Логин | Пароль по умолчанию |
|---|---|---|
| Сотрудник (E0028) | employee | career-demo-2026 |
| HR | hr | career-demo-2026 |

На странице входа есть кнопки для обеих демо-ролей. Данные синтетические. Стандартная конфигурация рассчитана на локальную демонстрацию.

```sh
docker compose ps
docker compose logs --tail=50 web
docker compose down
```

Если порт занят, задайте `APP_PORT=8001` в `.env` и откройте http://localhost:8001. `down` сохраняет данные; не добавляйте `-v`, если хотите их сохранить.

## Архитектура

### Общая схема программы

![Общая архитектура Career Quest](docs/images/prog_arch.png)

### Сценарий сотрудника

![Сценарий сотрудника](docs/images/employee_arch.png)

### Сценарий HR

![Сценарий HR](docs/images/HR_arch.png)

## Подключение AI

Скопировать `.env.example` в `.env` (в PowerShell: `Copy-Item .env.example .env`). Указать локально:

```dotenv
OPENAI_API_KEY=ваш_API_ключ
OPENAI_MODEL=gpt-4.1-mini
```

Затем `docker compose up -d --force-recreate web`. Ключ не передаётся в браузер и не коммитится. Требуется именно API-ключ OpenAI Platform, а не промокод ChatGPT.

Без ключа или при ошибке провайдера приложение работает с явно помеченным резервным многофакторным подбором. Реальный LLM выбирает события и подтверждающие факты; сервер проверяет их допустимость. Модель настраивается через `.env`.

## Локальная разработка: Windows / PowerShell

Python 3.12, виртуальная среда, база на выбор:

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
Copy-Item .env.example .env
.\scripts\run-local.ps1
```

Без DB_HOST используется SQLite для быстрого локального старта. Для PostgreSQL:

```powershell
docker compose up -d db
```

В `.env` задать `DB_HOST=localhost`, `DB_PORT=5433`, затем перезапустить локальное приложение. PostgreSQL/Compose используют одну схему моделей и миграций; интеграционные тесты запускаются на обеих БД. Локальный сервер: http://127.0.0.1:8000.

## Проверки

```powershell
.\.venv\Scripts\python.exe manage.py test
.\.venv\Scripts\python.exe manage.py check
.\.venv\Scripts\ruff.exe check .
```

В контейнере на PostgreSQL:

```sh
docker compose exec web python manage.py test
```

Тесты используют отдельную тестовую базу. Покрывают пересчёт после аттестации, потолки навыков, допустимость мероприятий, дополнительный импорт, атомарность ошибок, права и CSRF, идемпотентность завершения, проверку ответа AI и резервный режим. Вызовы модели в тестах заменены контролируемыми ответами: это не live-проверка API.

## Импорт и дата данных

- Исходный набор в `data/`: 200 профилей, 40 мероприятий, 60 навыков, 32 профиля требований, 2 743 записи истории.
- Дата среза: **2026-10-01**; она используется вместо системной даты.
- HR → «Загрузка данных»: дополнительные employees.json и activity_history.csv → проверить предпросмотр → «Подтвердить загрузку». Можно отдельно обновить каталог skills/events. Формат — как в [README датасета](data/README.ru.md).
- Весь импорт — одна транзакция. Повторные ID в файле отклоняются; существующие записи обновляются по ID. Повторный импорт не дублирует историю.
- CLI: `python manage.py import_dataset --directory path/to/files`.
- Дополнительные профили сразу доступны HR; отдельная учётная запись сотрудника автоматически не создаётся.
- Предпросмотр не изменяет профили, историю и каталог. После изменения затронутых записей или другого импорта старое подтверждение отклоняется. Повторное подтверждение уже выполненной загрузки безопасно. CLI выполняет импорт сразу.

## Архитектура и решения

Python 3.12 · Django 5.2 LTS · DRF · PostgreSQL 16 · Django Templates / Bootstrap grid / JavaScript · OpenAI Responses / Structured Outputs · Docker Compose / Waitress / WhiteNoise.

Слои: `quest/services/importer.py` — проверка, предпросмотр и загрузка; `career.py` — навыки, ограничения, признаки и HR; `plans.py` — переходы личного плана; `recommendations.py` — AI и проверка ответа; `views.py` — страницы/API и права. Ключевые модели: Employee, Skill, RoleProfile, Event, Participation, DevelopmentPlanItem, ImportDraft, RecommendationCache.

- [Архитектура, формулы и ограничения](docs/ARCHITECTURE.md)
- [Сценарий демонстрации и работа команды](docs/DEMO.md)
- [Результаты проверок и измерений](docs/VERIFICATION.md)

## Что важно для оценки

Пример E0028: System Design пересчитывается с 2 до 3 после EV_006. EV_005 не даёт дальнейшего прироста и исключается. Для Senior нужен уровень 4, но доступного события для этого разрыва сейчас нет — приложение показывает ограничение каталога. Другие подходящие шаги учитывают историю и пользу для цели.

Employee видит только свой профиль и план; HR видит команду, планы в режиме просмотра и импорт. Публичного рейтинга нет. В плане «Начать в демо» и «Завершить в демо» моделируют прохождение на дату среза, включая будущие сессии, и не подтверждают реальное обучение. При DEMO_MODE=0 симуляция отключается, планирование остаётся доступным. Подробные допущения — в архитектуре.

Прототип не содержит промышленную SSO/LMS-интеграцию, полную казахскую локализацию и измеренный бизнес-эффект. Правила резервного ранжирования — гипотезы команды. Задержку live-AI необходимо проверять с рабочим ключом.

## Проверка на другом устройстве

Для запуска у коллеги достаточно клонировать официальный репозиторий и выполнить Compose-команду. Для доступа к вашему компьютеру по общей сети задайте `HOST_BIND=0.0.0.0` и добавьте IP компьютера в `DJANGO_ALLOWED_HOSTS`, сохранив localhost и 127.0.0.1, затем пересоздайте web. Используйте только в сети команды: демо-пароли приведены выше.

## Источники и внешние компоненты

Данные синтетические, выданы организатором для хакатона; не предназначены для публикации вне его контура. Исходные README набора сохранены в `data/`. Bootstrap grid 5.3.3 используется по MIT, уведомление сохранено в CSS; удалена только ссылка на отсутствующую source map. Код приложения подготовлен с помощью Codex. Сторонний готовый проект Career Quest не использовался.

OpenAI: [Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs), [модель GPT-4.1 mini](https://developers.openai.com/api/docs/models/gpt-4.1-mini).
