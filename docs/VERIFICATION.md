# Проверка прототипа · 23.09.2026

## Выполнено

- 68 автоматических тестов проходят на SQLite и PostgreSQL 16 в Docker (финальная полировка).
- Django system check, отсутствие незаписанных миграций, Ruff и `git diff --check` проходят.
- Контейнеры web и db запускаются одной Compose-командой; healthcheck обеих служб успешен.
- Браузер: вход сотрудником и HR, профиль E0028, карточки рекомендаций, HR-агрегаты, форма импорта.
- Через форму повторно загружены 200 сотрудников: обработано 200, новых 0.
- В браузере завершён Cloud Certification Prep (EV_009) для E0028: покрытие навыков изменилось с 72% до 74%. Действие записано как симуляция в демонстрационной базе PostgreSQL; исходные файлы data/ не менялись.
- Мобильный профиль и карточки просмотрены при ширине 390 px.
- `.env`, `.venv`, SQLite и логи исключены из Git.

## Второй этап

- Полный цикл в браузере на отдельном тестовом стенде: подробности Mentor Track → добавить в план → начать → завершить. Покрытие E0028 изменилось 72% → 74%; до завершения навыки не начислялись.
- Автотесты проверяют повторное добавление/начало/завершение, удаление ещё не начатого шага, невозможность перескочить этапы, права сотрудника и HR, DEMO_MODE=0.
- Смена цели заново проверяет запланированные шаги; начатое обучение можно закончить. Проверены повторные пропуски и полностью достигнутые требования цели.
- Новый синтетический профиль JUDGE_STAGE2_QA загружен через браузер на отдельном стенде: предпросмотр показывает 1 новую запись, после подтверждения доступна ссылка и подбор для профиля.
- Автотесты проверяют отсутствие изменений до подтверждения, точные количества новых/обновляемых/неизменённых записей, отказ при устаревшем предпросмотре, доступ только владельцу черновика, срок 30 минут и повторное подтверждение без повторного импорта.
- HR-фильтры Middle + Cloud Platforms + «Нужна поддержка» дают согласованные список и агрегаты. Пустая выборка и поиск по ID также проверены.
- План, карточка мероприятия и HR-фильтры просмотрены на ширине 390 px; горизонтального переполнения страницы нет.
- В основной PostgreSQL-базе сохранены три демо-завершения E0028; миграция показывает их как 3 из 3 завершённых шагов. Тестовый профиль жюри и новое демо-прохождение создавались только на отдельном стенде.
- Финальная Docker-сборка: web и db healthy, миграции 0002–0004 применены. История и данные команды не сбрасывались.

## Третий этап: результат практики

- 60 тестов: SQLite — 7,1 с; PostgreSQL 16 — 93,9 с. Проверены запрос и публикация практики, ограничения прироста, персональный доступ, самопроверка, обязательные критерии и комментарий, возврат, повторная отправка, устаревшие формы, повторная проверка без начисления, безопасные ссылки и экранирование текста.
- Прогноз плана проверен отдельно от фактических навыков: не записывает прирост в историю. Импорт не позволяет перезаписать отправленную работу или подтвердить персональную практику в обход HR.
- Полный браузерный сценарий на отдельной SQLite-базе: запрос System Design → утверждение задания HR → добровольное добавление → начало → версия 1 → возврат с комментарием → версия 2 → подтверждение четырёх критериев. Обе версии сохранены; навык вырос 3 → 4, покрытие цели 72% → 74%. После этого фокус сменился на Cloud Platforms.
- Рабочая страница результата просмотрена на 390 px; профиль, запросы и HR-очередь проверены на отсутствие горизонтального переполнения.
- Вызовы провайдера в этом браузерном сценарии отключены; он проверяет практику и HR, а не генерацию или оценивание моделью.
- Основная Docker-версия пересобрана; web/db healthy, миграция 0005 применена. Проверено сохранение 200 сотрудников, 40 исходных событий, 2 746 записей истории, трёх прежних завершений E0028 и покрытия 78%. Тестовые запрос и работа в основную базу не добавлялись.
- Обновлены README, архитектура и сценарий защиты. Прежние демо-завершения доступны как история без проверки новой версии.

## Финальная полировка

- 68 тестов: SQLite — 10,5 с; PostgreSQL 16 в финальном образе — 31,3 с. Добавлены проверки входа, безопасного возврата на страницу, выбора демо-роли, выхода, истёкшей сессии, устаревшего CSRF и разных причин пустого подбора.
- В браузере проверены неверный пароль с сохранением логина, показ/скрытие пароля, вход обеих ролей, выход в другой вкладке и повторный вход по уведомлению. Демо-роль не наследует чужую страницу из параметра next.
- На отдельном стенде через форму загружены неизвестный профиль JUDGE_POLISH_01 и 3 записи no_show. Предпросмотр показал 1 новый профиль и 3 новые записи истории; после подтверждения профиль и объяснимые рекомендации доступны. В основную базу этот профиль не загружался.
- В отдельном стенде рекомендация Mentor Track добавлена в план и начата через браузер. До подтверждения результата фактические навыки не изменяются; прогноз показан отдельно.
- Карточки показывают численный прирост и факт из истории. Основной путь начинается с рекомендаций; индивидуальная практика — дополнительный сценарий. Роль HR и отсутствие отдельного назначения эксперта явно описаны.
- Один контролируемый реальный вызов GPT-4.1 mini: 4 437 мс, 2 допустимых шага EV_005 и EV_036, у каждого подтверждённые категории gap и history. Отправлены обезличенные признаки синтетического E0001. Вызов не менял навыки, историю и план; это один пример, не массовая оценка качества.
- Финальная Compose-сборка запущена на 8001, web/db healthy; check, makemigrations --check, Ruff и git diff --check проходят. Сохранились 200 сотрудников, три прежних завершения E0028, покрытие 78% и System Design 3. Основная база используется командой, поэтому число новых практик и работ может меняться независимо от изолированного QA.

## Реальные HTTP-замеры

Финальная сборка Docker/Waitress + PostgreSQL: 5 последовательных запросов на каждую операцию. Измерения не включают отрисовку браузера и не являются нагрузочным тестом. Для E0028 новых исходных кандидатов в момент замера не было: строка подбора проверяет пустое состояние, а не скорость модели.

| Операция | Медиана | Максимум |
|---|---:|---:|
| HTML профиля | 61,1 мс | 70,9 мс |
| API профиля | 31,3 мс | 62,6 мс |
| Пустой подбор | 61,0 мс | 69,6 мс |
| HTML HR, 200 сотрудников | 253,4 мс | 454,9 мс |

### Предыдущие замеры

Замеры первой версии до второго этапа: локальная сборка Docker/Waitress + PostgreSQL, 5 последовательных запросов на каждую операцию. Эти измерения не включают отрисовку браузера и не являются нагрузочным тестированием. Для новых экранов второго этапа latency отдельно не измерялась.

| Операция | Медиана | Максимум |
|---|---:|---:|
| HTML профиля | 66,5 мс | 118,2 мс |
| API профиля | 29,2 мс | 39,3 мс |
| Подбор по правилам, включая кэш | 18,6 мс | 65,5 мс |
| HTML HR, 200 сотрудников | 171,3 мс | 240,0 мс |

Повторить: `python scripts/smoke_http.py --base-url http://127.0.0.1:8001` (либо порт вашей сборки).

## Требует проверки с командой

- Качество выбора LLM на неизвестных профилях жюри и стабильность задержки до 10 с при повторных запросах/нагрузке. Один успешный live-замер описан выше; ответы провайдера в автоматических тестах заменены.
- Чистый запуск на другом физическом устройстве и командная репетиция защиты.
- Работа в общей сети команды; по умолчанию порты доступны только локально.

В текущем запуске основной просмотр в Docker: http://localhost:8001. `.env` на машине разработчика задаёт APP_PORT=8001; стандартный порт нового клона без этой настройки — 8000.
