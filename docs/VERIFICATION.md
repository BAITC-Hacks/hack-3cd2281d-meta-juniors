# Проверка прототипа · 23.09.2026

## Выполнено

- 30 автоматических тестов проходят на SQLite и PostgreSQL 16 в Docker.
- Django system check, отсутствие незаписанных миграций, Ruff и `git diff --check` проходят.
- Контейнеры web и db запускаются одной Compose-командой; healthcheck обеих служб успешен.
- Браузер: вход сотрудником и HR, профиль E0028, карточки рекомендаций, HR-агрегаты, форма импорта.
- Через форму повторно загружены 200 сотрудников: обработано 200, новых 0.
- В браузере завершён Cloud Certification Prep (EV_009) для E0028: покрытие навыков изменилось с 72% до 74%. Действие записано как симуляция в демонстрационной базе PostgreSQL; исходные файлы data/ не менялись.
- Мобильный профиль и карточки просмотрены при ширине 390 px.
- `.env`, `.venv`, SQLite и логи исключены из Git.

## Реальные HTTP-замеры

Локальная сборка Docker/Waitress + PostgreSQL, 5 последовательных запросов на каждую операцию. Эти измерения не включают отрисовку браузера и не являются нагрузочным тестированием.

| Операция | Медиана | Максимум |
|---|---:|---:|
| HTML профиля | 66,5 мс | 118,2 мс |
| API профиля | 29,2 мс | 39,3 мс |
| Подбор по правилам, включая кэш | 18,6 мс | 65,5 мс |
| HTML HR, 200 сотрудников | 171,3 мс | 240,0 мс |

Повторить: `python scripts/smoke_http.py --base-url http://127.0.0.1:8001` (либо порт вашей сборки).

## Требует проверки с командой

- Реальный вызов OpenAI API: ключ пока не подключён. Автотесты проверяют интеграционную логику с заменой ответа провайдера, а не доступ к сервису.
- Качество выбора LLM на профилях жюри и задержка реальной AI-рекомендации до 10 с.
- Чистый запуск на другом физическом устройстве и командная репетиция защиты.
- Работа в общей сети команды; по умолчанию порты доступны только локально.

В текущем запуске основной просмотр в Docker: http://localhost:8001. `.env` на машине разработчика задаёт APP_PORT=8001; стандартный порт нового клона без этой настройки — 8000.

